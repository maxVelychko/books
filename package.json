{
  "name": "books",
  "scripts": {
    "start": "cd server && yarn start",
    "heroku-postbuild": "yarn & (cd server && yarn tsc) & (cd client && yarn && yarn build)",
    "dev": "(cd server && yarn dev) & (cd client && yarn start)"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.1",
    "env-cmd": "^10.1.0",
    "nodemon": "^2.0.4",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.2"
  },
  "dependencies": {
    "express": "^4.17.3",
    "mongoose": "^5.9.15"
  }
}
